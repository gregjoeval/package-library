{
  "name": "root",
  "private": true,
  "scripts": {
    "init": "lerna run init && lerna bootstrap --hoist --strict",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "lint:fix": "lerna run lint:fix",
    "typecheck": "lerna run typecheck",
    "test": "lerna run test",
    "check": "npm run lint && npm run typecheck && npm run test",
    "publish:ci": "lerna publish --canary --preid next --no-git-reset --no-verify-access --yes"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
